
#!/usr/bin/env bash
# File: addseq.sh

function plier {
 local p=1

  for element in $@
  do
    p=$(echo "$p * $element" | bc -l )
  done

  echo $p
}
#Checking odd or even

function isiteven 
{

#!/usr/bin/env bash
# File: addseq.sh

function addseq {
  sum=0

  for element in $@
  do
    sum=$(echo "$sum + $element" | bc -l )
  done

  echo $sum
}
66666666666666666
then
echo "True"
else
echo "False"
fi
}


